name: Test Org Runner

on:
  workflow_dispatch:  # so you can manually trigger it

jobs:
  test:
    runs-on: [self-hosted, Linux, X64]  # match your runner labels
    steps:
      - name: Show runner info
        run: |
          echo "Org-level runner picked up this job!"
          echo "Machine name: $(hostname)"
          uname -a
